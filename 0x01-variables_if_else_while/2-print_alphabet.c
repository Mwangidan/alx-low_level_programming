#include <stdio.h>
/**
 * mani - A program that prints the alphabet in lowercase
 * Return: Always 0 (Pass)
 */
int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
			putchar(c);
			c++;

		}
	putchar('\n');
	return (0);
}
